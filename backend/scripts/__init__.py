"""Dev-tooling scripts (universe loaders, backfills).

Exposed as an importable package so unit tests can import a script's pure
helpers with a fully-qualified path (``from scripts.<module> import ...``)
that resolves natively — no injected source root required.
"""
