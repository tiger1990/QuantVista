"""jobs — Celery app + Beat schedule (worker/beat roles).

Composition root: may import any context's interfaces; nothing imports jobs.
Task wiring lands in QV-015+ (kept empty here per QV-001 scope).
"""
